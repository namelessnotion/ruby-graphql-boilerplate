#!/usr/bin/env ruby
# frozen_string_literal: true
# typed: strict

require_relative '../app/app'
require_relative '../lib/memory_snapshot'
require_relative '../lib/memory_workload'

request = Rack::MockRequest.new(App.new)

GC.start
before = MemorySnapshot.rss_mb

MemoryWorkload.run(request)

GC.start
after = MemorySnapshot.rss_mb

puts after - before
